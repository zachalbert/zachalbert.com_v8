/**
 * Site-wide feature flags
 * Toggle features on/off by changing these values
 */

export const siteConfig = {
  /**
   * Toggle the writing/blog section across the entire site
   * When false:
   * - Writing section is hidden from homepage
   * - Writing link is removed from navigation
   * - Individual article routes return 404
   */
  showWriting: false,
};
